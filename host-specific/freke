if [ -e /usr/share/terminfo/s/screen-256color-bce-s ]; then
	export TERM='screen-256color-s'
else
	export TERM='xterm-color'
fi

# Load hub.
eval "$(hub alias -s)"

keychain id_rsa -q
. ~/.keychain/`uname -n`-sh
